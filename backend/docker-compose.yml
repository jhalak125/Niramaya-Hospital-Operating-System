version: "3.8"


services:

  backend:

    build: .

    container_name: hospital_backend

    ports:
      - "8000:8000"


    env_file:
      - .env


    volumes:
      - ./uploads:/app/uploads


    restart: always